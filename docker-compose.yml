x-common-config: &common-config
  build:
    context: .
    dockerfile: Dockerfile
  volumes:
    - ~/.nanobot:/root/.nanobot

services:
  nanobot-gateway:
    container_name: nanobot-gateway
    <<: *common-config
    command: ["gateway"]
    restart: unless-stopped
    environment:
      # Use Debian system CA bundle for Python requests/urllib3 inside the container.
      SSL_CERT_FILE: /etc/ssl/certs/ca-certificates.crt
      REQUESTS_CA_BUNDLE: /etc/ssl/certs/ca-certificates.crt
      CURL_CA_BUNDLE: /etc/ssl/certs/ca-certificates.crt
    ports:
      - 18790:18790
    deploy:
      resources:
        limits:
          cpus: '1'
          memory: 1G
        reservations:
          cpus: '0.25'
          memory: 256M

  nanobot-webui:
    container_name: nanobot-webui
    <<: *common-config
    profiles:
      - webui
    command: ["webui", "--host", "0.0.0.0", "--port", "18791"]
    restart: unless-stopped
    environment:
      # Optional: set in shell before `docker compose up` to enable Web UI auth.
      NANOBOT_WEBUI_TOKEN: ${NANOBOT_WEBUI_TOKEN:-}
      # Use Debian system CA bundle for Python requests/urllib3 inside the container.
      SSL_CERT_FILE: /etc/ssl/certs/ca-certificates.crt
      REQUESTS_CA_BUNDLE: /etc/ssl/certs/ca-certificates.crt
      CURL_CA_BUNDLE: /etc/ssl/certs/ca-certificates.crt
    ports:
      - 18791:18791
    deploy:
      resources:
        limits:
          cpus: '0.50'
          memory: 512M
        reservations:
          cpus: '0.10'
          memory: 128M
  
  nanobot-cli:
    <<: *common-config
    profiles:
      - cli
    command: ["status"]
    environment:
      SSL_CERT_FILE: /etc/ssl/certs/ca-certificates.crt
      REQUESTS_CA_BUNDLE: /etc/ssl/certs/ca-certificates.crt
      CURL_CA_BUNDLE: /etc/ssl/certs/ca-certificates.crt
    stdin_open: true
    tty: true
